"use client";

/**
 * Geteilte UI-Konstanten der KI-Einheit v3.
 */

/**
 * Wartezeit (ms) vor dem automatischen Weiterspringen nach einer diskreten
 * Antwort (4er-Skala, Werte-Karte). Bewusst etwas länger (#2), damit die
 * getroffene Auswahl sichtbar bleibt, bevor der nächste Frame einblendet.
 */
export const AUTO_ADVANCE_MS = 850;
